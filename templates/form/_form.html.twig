
{{ form_start(form, { attr: attr|default({}) }) }}
{{ form_widget(form) }}

<input type="submit" value="{{ button_label|default('create'|trans) }}"
       class="{{ button_css|default("btn btn-primary") }}" />

{{ form_end(form) }}

